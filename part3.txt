1.
It is the mechanism that lets Node.js execute async and non-blocking operations on a single thread

2.
Libuv is a library that powers Node.js’s event loop and thread pool

3.
through event loop, libuv and os

4.
Call Stack: Executes JS code one function at a time

Event Queue: Holds callbacks waiting to be executed

Event Loop: Moves tasks from the queue to the stack when the stack is empty


5.
A group of libuv background threads used for heavy async tasks (fs, crypto)

set siza by: export UV_THREADPOOL_SIZE=8

Default size = 4

6.
Non-blocking: Uses event loop + libuv; main thread continues

Blocking: Runs on main thread and stops everything until done